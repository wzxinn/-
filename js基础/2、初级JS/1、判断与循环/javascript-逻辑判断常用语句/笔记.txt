for

continue - 跳过当前循环

隔行变色 

i 是 偶数的时候 是咱们想让他跳过的状态

break - 停止循环

while(条件){
	执行语句
}

至少会执行一次
do{
	执行语句
}
while(条件);

swicth(变量){
	case 变量的值:
	 脚本语句
	break;
	case 变量的值:
	 脚本语句
	break;
	case 变量的值:
	 脚本语句
	break;
	case 变量的值:
	 脚本语句
	break;
	default:
	 脚本语句;
	break;
}

如果不加break就会穿透到 有break 停止


if(){
	
}

else if(){
	
}
else{
	
}


&& 和 多个判断条件是否符合 一般用在if里面
只要条件不满足 就不往后看了
但是条件满足 就会一直往后看

|| 或 只要满足其中一个条件 就可以
只要满足其中一个条件 就不往后看了
但是条件不满足 就会一直往后看




